import sys as s
import json

def load_data():
    with open("data.json", "r", encoding="utf-8") as file:
        return json.load(file)

def save_data(data):
    with open("data.json", "w", encoding="utf-8") as file:
        json.dump(data, file, indent=4)

se = "                               "
tl = 40

def garis():
    print("==============================================")

def garis_pinggir():
    print("=                                            =")

def garis_stok():
    print("===============================================================")

def judul():
    garis()
    garis_pinggir()
    print("=   Selamat Datang di Perpustakaan SMAN 3    =")
    garis_pinggir()
    garis()

def enter():
    print(" ")

def print_buku():
    no = 1
    dd = 0
    for buku, jumlah in daftar_buku.items():
        if no > 9:
            dd += 1
        print(f"= {no}. {buku}{se[:tl-len(buku) - dd]}= Tersedia: {jumlah}    =")
        no += 1

def tunjuk_buku():
    enter()
    enter()
    garis_stok()
    print("= Daftar Buku                                = Stok Buku      =")
    garis_stok()
    print_buku()
    garis_stok() 
    enter()

def tunjuk_buku_pinjam():
    enter()
    enter()
    garis_stok()
    print("= Daftar buku yang dapat dipinjam            = Stok Buku      =")
    garis_stok()
    print_buku()
    garis_stok() 
    enter()

daftar_buku = load_data()

keranjang = []
 
def menu_perpus():
    garis()
    print("= MENU PERPUSTAKAAN                          =")
    garis()
    print("= 1. Lihat daftar buku                       =")
    print("= 2. Pinjam buku                             =")
    print("= 3. Lihat keranjang                         =")
    print("= 4. Kasir / Proses peminjaman               =")
    print("= 5. Keluar                                  =")
    garis()

def pilihan():
    garis()
    print("=      Silahkan pilih dalam bentuk angka     =")
    garis()

def pinjam():
    while True:

        tunjuk_buku_pinjam()

        try:
            daftar_nama = list(daftar_buku.keys())
            pilih = int(input("Pilih nomor buku: "))
            

            if pilih < 1 or pilih > len(daftar_nama):
                print("Pilihan tidak valid.")
                input("Tekan ENTER untuk memilh ulang...")
                continue

            buku = daftar_nama[pilih -1]

            if daftar_buku[buku] <= 0:
                print("Maaf, buku yang anda minta sudah habis")
                input("Tekan ENTER untuk memilih ulang...")
                continue

            else:
                if buku in keranjang:
                    print("Buku sudah ada di keranjang.")
                else:
                    keranjang.append(buku)
                    print("Buku berhasil ditambahkan.")

        except ValueError:
            print("Tolong masukkan angka! ")
            input("Tekan ENTER untuk memilih ulang...")
            continue

        lagi = input("Pilih buku lagi (y/t)? ")
        if lagi.lower() != "y":
            break
   
def lihat_keranjang():
    enter()
    enter()
    garis()
    print("=                 KERANJANG                  =")
    garis()
    garis_pinggir()

    if len(keranjang) == 0:
        print("= Keranjang masih kosong                     =")
    else:
        no = 1
        dd = 0
        for buku in keranjang:
            if no > 9:
              dd += 1
            print(f"= {no}. {buku}{se[:tl-len(buku) - dd]} =")
            no += 1

    garis_pinggir()
    garis()

def kasir():
    while True:
        enter()
        enter()
        garis()
        print("=                  KASIR                     =")
        garis()

        if len(keranjang) == 0:
            print("= Keranjang kosong, tidak ada buku         =")
            garis()
            input("Tekan ENTER untuk kembali...")
            return

        print("= Buku yang akan dipinjam:                   =")
        garis()
        garis_pinggir()
        no = 1
        dd = 0
        for buku in keranjang:
            if no > 9:
                dd += 1
            print(f"= {no}. {buku}{se[:tl-len(buku) - dd]}=")
            no += 1

        garis_pinggir()
        garis()
    
        konfirmasi = input("Konfirmasi peminjaman? (y/t): ")
        if konfirmasi.lower() == "y":
            for buku in keranjang:
                daftar_buku[buku] -= 1

            save_data(daftar_buku)

            keranjang.clear()
            enter()
            enter()
            garis()
            garis_pinggir()
            print("=           Peminjaman berhasil              =")
            garis_pinggir()
            garis()
            s.exit()
        elif konfirmasi.lower() == "t":
            enter()
            enter()
            garis()
            garis_pinggir()
            print("=           Peminjaman dibatalkan            =")
            garis_pinggir()
            garis()
            input("Tekan ENTER untuk kembali ke menu...")
            enter()
            enter()
            return
        else:
            print("Mohon konfirmasi dengan memilh y/t")
            input("Tekan ENTER untuk kembali...")
            continue

judul()

while True:
    enter()
    menu_perpus()
    enter()
    pilihan()
    try:
        answer = int(input("Pilihan Anda: "))
    except ValueError:
        print("Tolong masukkan angka...")
        input("Tekan ENTER Untuk Kembali...")
        continue

    if answer == 1:
        tunjuk_buku()
        input("Tekan ENTER untuk kembali ke menu...")
    elif answer == 2:
        pinjam()
    elif answer == 3:
        lihat_keranjang()
        input("Tekan ENTER untuk kembali ke menu...")
    elif answer == 4:
        kasir()
    elif answer == 5:
        print("====================================================")
        enter()
        print("= Terima kasih telah mengunjungi perpustakaan kami =")
        print("=          Semoga hari Anda menyenangkan!          =")
        enter()
        print("====================================================")
        break
    else:
        print("Pilihan tidak tersedia.")
        input("Tekan ENTER untuk kembali ke menu...")import sys as s

def garis():
    print("==============================================")

def garis_pinggir():
    print("=                                            =")

def judul():
    garis()
    garis_pinggir()
    print("=   Selamat Datang di Perpustakaan SMAN 3    =")
    garis_pinggir()
    garis()

def enter():
    print(" ")

def print_buku():
    print("= 1. Dasar-Dasar Pemrograman Python          =\n"
          "= 2. Algoritma dan Logika Berpikir           =\n"
          "= 3. Pengantar Ilmu Komputer                 =\n"
          "= 4. Matematika Diskrit untuk Pelajar        =\n"
          "= 5. Sistem Operasi dan Cara Kerjanya        =\n"
          "= 6. Jaringan Komputer Dasar                 =\n"
          "= 7. Struktur Data Sederhana                 =\n"
          "= 8. Keamanan Data dan Informasi             =\n"
          "= 9. Kecerdasan Buatan untuk Pemula          =\n"
          "= 10. C++ Sampai bisa                        ="
          )

def tunjuk_buku():
    enter()
    enter()
    garis()
    print("= Daftar Buku                                =")
    garis()
    print_buku()
    garis() 
    enter()

def tunjuk_buku_pinjam():
    enter()
    enter()
    garis()
    print("= Daftar buku yang dapat dipinjam            =")
    garis()
    print_buku()
    garis() 
    enter()

daftar_buku = [
          "Dasar-Dasar Pemrograman Python          =",
          "Algoritma dan Logika Berpikir           =",
          "Pengantar Ilmu Komputer                 =",
          "Matematika Diskrit untuk Pelajar        =",
          "Sistem Operasi dan Cara Kerjanya        =",
          "Jaringan Komputer Dasar                 =",
          "Struktur Data Sederhana                 =",
          "Keamanan Data dan Informasi             =",
          "Kecerdasan Buatan untuk Pemula          =",
          "C++ Sampai bisa                         =",
    ]

keranjang = []
 
def menu_perpus():
    garis()
    print("= MENU PERPUSTAKAAN                          =")
    garis()
    print("= 1. Lihat daftar buku                       =")
    print("= 2. Pinjam buku                             =")
    print("= 3. Lihat keranjang                         =")
    print("= 4. Kasir / Proses peminjaman               =")
    print("= 5. Keluar                                  =")
    garis()

def pilihan():
    garis()
    print("=      Silahkan pilih dalam bentuk angka     =")
    garis()

def pinjam():
    while True:
        tunjuk_buku_pinjam()

        try:
            pilih = int(input("Pilih nomor buku: "))

            if pilih < 1 or pilih > len(daftar_buku):
                print("Pilihan tidak valid.")
                input("Tekan ENTER untuk memilh ulang...")
                continue
            
            buku = daftar_buku[pilih - 1]

            if buku in keranjang:
                print("Buku sudah ada di keranjang.")
            else:
                keranjang.append(buku)
                print("Buku berhasil ditambahkan.")

        except ValueError:
            print("Tolong masukkan angka! ")
            continue

        lagi = input("Pilih buku lagi (y/t)? ")
        if lagi.lower() != "y":
            break
   
def lihat_keranjang():
    enter()
    enter()
    garis()
    print("=                 KERANJANG                  =")
    garis()
    garis_pinggir()

    if len(keranjang) == 0:
        print("= Keranjang masih kosong                     =")
    else:
        no = 1
        for buku in keranjang:
            print(f"= {no}. {buku}")
            no += 1
    garis_pinggir()
    garis()

def kasir():
    while True:
        enter()
        enter()
        garis()
        print("=                  KASIR                     =")
        garis()

        if len(keranjang) == 0:
            print("= Keranjang kosong, tidak ada buku         =")
            garis()
            input("Tekan ENTER untuk kembali...")
            return

        print("= Buku yang akan dipinjam:                   =")
        garis()
        garis_pinggir()
        no = 1
        for buku in keranjang:
            print(f"= {no}. {buku}")
            no += 1
        garis_pinggir()
        garis()
    
        konfirmasi = input("Konfirmasi peminjaman? (y/t): ")
        if konfirmasi.lower() == "y":
            keranjang.clear()
            enter()
            enter()
            garis()
            garis_pinggir()
            print("=           Peminjaman berhasil              =")
            garis_pinggir()
            garis()
            s.exit()
        elif konfirmasi.lower() == "t":
            enter()
            enter()
            garis()
            garis_pinggir()
            print("=           Peminjaman dibatalkan            =")
            garis_pinggir()
            garis()
            s.exit()
        else:
            print("Mohon konfirmasi dengan memilh y/t")
            input("Tekan ENTER untuk kembali...")
            continue

judul()

while True:
    enter()
    menu_perpus()
    enter()
    pilihan()
    try:
        answer = int(input("Pilihan Anda: "))
    except ValueError:
        print("Tolong masukkan angka tuan...")
        input("Tekan ENTER Untuk Kembali...")
        continue

    if answer == 1:
        tunjuk_buku()
        input("Tekan ENTER untuk kembali ke menu...")
    elif answer == 2:
        pinjam()
    elif answer == 3:
        lihat_keranjang()
        input("Tekan ENTER untuk kembali ke menu...")
    elif answer == 4:
        kasir()
    elif answer == 5:
        print("Terima kasih telah mengunjungi perpustakaan kami")
        break
    else:
        print("Pilihan tidak tersedia.")
        input("Tekan ENTER untuk kembali ke menu...")
